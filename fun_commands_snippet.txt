// --- FUN COMMANDS ---
// Joke
if (command === 'joke' || command === 'نكتة') {
    const jokes = [
        "😂 واحد دخل للطبيب قال ليه: دكتور كنحس براسي خفيف. قال ليه: ماشي مشكل، غادي نزيدو ليك شوية ديال الرمل!",
        "🤣 علاش الهاتف ماكيبكيش؟ حيت عندو وضع الصامت!",
        "😄 شنو كيقول الكمبيوتر للكمبيوتر الآخر؟ 010101!",
        "😆 واحد سأل صاحبو: واش نتا ذكي؟ قال ليه: آه، ولكن مكنعرفش نقراها!",
        "🤪 علاش الدجاجة عبرات الطريق؟ باش توصل للجيهة الأخرى!"
    ];
    return callSendAPI(sender_psid, { text: jokes[Math.floor(Math.random() * jokes.length)] });
}

// Quote
if (command === 'quote' || command === 'حكمة') {
    const quotes = [
        "💡 'النجاح هو الانتقال من فشل إلى فشل دون فقدان الحماس' - ونستون تشرشل",
        "✨ 'الطريقة الوحيدة للقيام بعمل عظيم هي أن تحب ما تفعله' - ستيف جوبز",
        "🌟 'لا تخف من النمو البطيء، خف فقط من الوقوف' - مثل صيني",
        "🎯 'المستقبل ملك لأولئك الذين يؤمنون بجمال أحلامهم' - إليانور روزفلت",
        "💪 'القوة لا تأتي من القدرة الجسدية، بل من الإرادة التي لا تقهر' - غاندي"
    ];
    return callSendAPI(sender_psid, { text: quotes[Math.floor(Math.random() * quotes.length)] });
}

// Dice
if (command === 'dice' || command === 'زهر') {
    const result = Math.floor(Math.random() * 6) + 1;
    return callSendAPI(sender_psid, { text: `🎲 *النتيجة:* ${result}` });
}

// Truth or Dare
if (command === 'truthordare' || command === 'tod' || command === 'صراحة') {
    const truths = [
        "🤔 شنو أكبر كذبة قلتيها؟",
        "😳 واش عندك كراش على شي حد؟",
        "🙈 شنو أكثر حاجة كتندم عليها؟",
        "😅 شنو أغرب حلم شفتي؟"
    ];
    const dares = [
        "😎 غني أغنية بصوت عالي!",
        "🤪 سيفط رسالة لأول شخص في جهات الاتصال!",
        "😂 قول نكتة!",
        "🎭 قلد شي حد تعرفو!"
    ];
    const choice = Math.random() > 0.5;
    const list = choice ? truths : dares;
    const type = choice ? "صراحة" : "جرأة";
    return callSendAPI(sender_psid, { text: `*${type}:* ${list[Math.floor(Math.random() * list.length)]}` });
}
